[tool.poetry]
name = "newflaskproject"
version = "0.1.0"
description = ""
authors = ["Timur <vodovozzz@inbox.ru>"]

[tool.poetry.dependencies]
python = "^3.7"
Flask = "^2.2.3"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"